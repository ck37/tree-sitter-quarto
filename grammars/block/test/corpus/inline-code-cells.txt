==================
Python inline code cell
==================

The result is `{python} 2 + 2`.

---

(document
  (blank_line)
  (paragraph
    content: (inline)))

==================
R shorthand syntax
==================

The mean is `r mean(x)` here.

---

(document
  (blank_line)
  (paragraph
    content: (inline)))

==================
Julia inline code cell
==================

Compute `{julia} sqrt(144)` quickly.

---

(document
  (blank_line)
  (paragraph
    content: (inline)))

==================
JavaScript inline code cell
==================

Value: `{javascript} Math.PI * 2`.

---

(document
  (blank_line)
  (paragraph
    content: (inline)))

==================
Multiple inline cells
==================

Add `{python} x + y` to `{python} a * b` for total.

---

(document
  (blank_line)
  (paragraph
    content: (inline)))

==================
Complex expression
==================

The value `{python} np.mean([1, 2, 3])` is shown.

---

(document
  (blank_line)
  (paragraph
    content: (inline)))

==================
R shorthand with function call
==================

Result: `r sum(data$column)` total.

---

(document
  (blank_line)
  (paragraph
    content: (inline)))

==================
Inline cell vs regular code span
==================

Use `{python} x` not `regular code` here.

---

(document
  (blank_line)
  (paragraph
    content: (inline)))

================================================================================
Empty inline code cell
================================================================================

An empty cell: `{python}` here.

--------------------------------------------------------------------------------

(document
  (blank_line)
  (paragraph
    content: (inline)))
